# This is a template for the environment variables.
# Your teammate should copy this file to a new file named .env
# and then fill in the secret values below.

# The connection string for the PostgreSQL database.
# This needs to be obtained from the database provider (e.g., Vercel, Supabase, etc.).
DATABASE_URL="YOUR_DATABASE_CONNECTION_STRING_HERE"

# A secret key used by NextAuth to sign tokens and cookies.
# A new one can be generated by running this command in the terminal:
# openssl rand -base64 32
NEXTAUTH_SECRET="YOUR_NEXTAUTH_SECRET_HERE"

# The full URL of your application, used for redirects by NextAuth.
# For local development, this is typically http://localhost:3000.
NEXTAUTH_URL="http://localhost:3000"